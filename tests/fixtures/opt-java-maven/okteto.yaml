# Intentionally un-optimized starting manifest for the manifest-optimizer eval.
# :latest, hardcoded dev image, full-tree sync, no build/volumes/resources/test/ignores.
dev:
  catalog:
    image: maven:latest
    command: bash
    sync:
      - .:/app
